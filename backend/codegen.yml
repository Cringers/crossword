overwrite: true
schema: "./typeDefs.ts"
documents: "**/*.graphql"
generates:
  ./generated/graphql.ts:
    plugins:
      - "typescript"
      - "typescript-resolvers"
      - "typed-document-node"
  ./graphql.schema.json:
    plugins:
      - "introspection"
