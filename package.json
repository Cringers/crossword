{
  "name": "crossword",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend",
    "packages/*"
  ],
  "scripts": {
    "start": "yarn --cwd backend dev & yarn --cwd frontend start",
    "build": "yarn --cwd frontend build",
    "generate": "yarn --cwd frontend generate"
  }
}
